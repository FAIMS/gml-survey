import android.util.Log;

Object dialog;          // Used to help coordinate the display of a "busy..." dialog
String parentTabgroup;  // Used to allow entities to be saved as children
String redirectTab;     // makes newTab work as expected
String username = "";

setFileSyncEnabled(true);
setSyncDelay(5.0f);
setSyncEnabled(true);
setSyncMaxInterval(600.0f);
setSyncMinInterval(5.0f);

makeLocalID(){
  fetchOne("CREATE TABLE IF NOT EXISTS localSettings (key text primary key, value text);", null);
  fetchOne("DROP VIEW IF EXISTS parentchild;", null);
  fetchOne("CREATE VIEW parentchild AS "+
           "SELECT parent.uuid as parentuuid, child.uuid as childuuid, parent.participatesverb as parentparticipatesverb, parent.relationshipid, parent.aenttypename as parentaenttypename, child.participatesverb as childparticipatesverb, child.aenttypename as childaenttypename "+
           "  FROM (SELECT uuid, participatesverb, aenttypename, relationshipid"+
           "          FROM latestnondeletedaentreln "+
           "          JOIN relationship USING (relationshipid) "+
           "          JOIN latestnondeletedarchent USING (uuid) "+
           "          JOIN aenttype USING (aenttypeid)) parent "+
           "  JOIN (SELECT uuid, relationshipid, participatesverb, aenttypename "+
           "          FROM latestnondeletedaentreln "+
           "          JOIN relationship USING (relationshipid) "+
           "          JOIN latestnondeletedarchent USING (uuid) "+
           "          JOIN aenttype USING (aenttypeid)) child "+
           "    ON (parent.relationshipid = child.relationshipid AND parent.uuid != child.uuid);", null);
}
makeLocalID();

insertIntoLocalSettings(String key, String val) {
  fetchOne("REPLACE INTO localSettings(key, value) VALUES('" + key + "', '" + val + "');");
}

insertIntoLocalSettings(String key, Integer val) {
  insertIntoLocalSettings(key, Integer.toString(val));
}

setFieldValueFromLocalSettings(String key, String ref) {
  String q = "SELECT value FROM localSettings WHERE key = '" + key + "';";
  fetchOne(q, new FetchCallback() {
    onFetch(result) {
      if (!isNull(result)) {
        setFieldValue(ref, result.get(0));
      }
    }
  });
}

newTab(String tab, Boolean resolveTabGroups) {
  if (!resolveTabGroups) {
    return newTab(tab);
  }

  tab = tab.replaceAll("/$", "");
  tab = tab.replaceAll("^/", "");

  path = tab.split("/");
  switch (path.length) {
    case 0:
      break;
    case 1:
      newTabGroup(path[0]);
      break;
    case 2:
      String tabgroupString = path[0];
      String tabString      = path[0] + "/" + path[1];

      showWarning(tabString, tabString);

      redirectTab = tabString;
      String onShowTabgroup = "if (!isNull(redirectTab)) { newTab(redirectTab); redirectTab = \"\"; }";
      addOnEvent(tabgroupString, "show", onShowTabgroup);

      newTabGroup(tabgroupString);
      newTab(tabString);
      break;
    default:
  }
}

/******************************************************************************/
/*                            BINDING ACCUMULATOR                             */
/*                                                                            */
/* Allows onEvent bindings for the same element to accumulate over multiple   */
/* onEvent calls instead of having later calls override earlier ones.         */
/******************************************************************************/
Map events = new HashMap();
String SEP = Character.toString ((char) 0); // Beanshell is stupid and won't let me write "\0"

/* Returns the set of statements bound to an element at `ref` and occuring on
 * `event`.
 */
getStatements(String ref, String event) {
  String    key = ref + SEP + event;
  ArrayList val = (ArrayList) events.get(key);
  if (val == null) {
    val = new ArrayList();
    events.put(key, val);
  }
  return val;
}

addOnEvent(String ref, String event, String statement) {
  // Calling `remove()` first ensures statement occurs once in the list, at the end.
  while(getStatements(ref, event).remove(statement));
  getStatements(ref, event).add(statement);
}

delOnEvent(String ref, String event, String statement) {
  while(getStatements(ref, event).remove(statement));
}

bindOnEvent(String ref, String event) {
  ArrayList stmts = getStatements(ref, event);
  String stmtsExpr = "";
  for (String s : stmts) {
    stmtsExpr += s;
    stmtsExpr += "; ";
  }

  onEvent(ref, event, stmtsExpr);
}

bindOnEvents() {
  for (String key : events.keySet()) {
    refevent = key.split(SEP);
    ref   = refevent[0];
    event = refevent[1];
    bindOnEvent(ref, event);
  }
}

/******************************************************************************/
/*                           DROPDOWN VALUE GETTER                            */
/*                                                                            */
/* For consistency with `getListItemValue()`.                                 */
/******************************************************************************/
String dropdownItemValue = null;

getDropdownItemValue() {
  return dropdownItemValue;
}

addOnEvent("Transect/Transect/Landform", "click", "dropdownItemValue = getFieldValue(\"Transect/Transect/Landform\")");
addOnEvent("Transect/Transect/Slope_Gradient", "click", "dropdownItemValue = getFieldValue(\"Transect/Transect/Slope_Gradient\")");
addOnEvent("Transect/Transect/Geomorphological_Activity", "click", "dropdownItemValue = getFieldValue(\"Transect/Transect/Geomorphological_Activity\")");
addOnEvent("Site/Site/Site_Aspect_Orientation", "click", "dropdownItemValue = getFieldValue(\"Site/Site/Site_Aspect_Orientation\")");
addOnEvent("Site/Closed_Site/Shelter_Cave_Formation", "click", "dropdownItemValue = getFieldValue(\"Site/Closed_Site/Shelter_Cave_Formation\")");
addOnEvent("Site/Closed_Site/Rock_Surface_Condition", "click", "dropdownItemValue = getFieldValue(\"Site/Closed_Site/Rock_Surface_Condition\")");
addOnEvent("Feature/Feature/Feature_Condition", "click", "dropdownItemValue = getFieldValue(\"Feature/Feature/Feature_Condition\")");
addOnEvent("Artefact/Artefact/Insitu_Artefact", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Insitu_Artefact\")");
addOnEvent("Artefact/Artefact/Stratified_Artefact", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Stratified_Artefact\")");
addOnEvent("Artefact/Artefact/Artefact_Raw_Material", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Artefact_Raw_Material\")");
addOnEvent("Artefact/Artefact/Artefact_Type", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Artefact_Type\")");
addOnEvent("Artefact/Artefact/Artefact_Description_for_Aboriginal_Stone_Artefacts", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Artefact_Description_for_Aboriginal_Stone_Artefacts\")");
addOnEvent("Artefact/Artefact/Artefact_Colour", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Artefact_Colour\")");
addOnEvent("Artefact/Artefact/Artefact_Status_Modification", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Artefact_Status_Modification\")");
addOnEvent("Artefact/Artefact/Platform_Surface_for_Aboriginal_stone_artefacts", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Platform_Surface_for_Aboriginal_stone_artefacts\")");
addOnEvent("Artefact/Artefact/Platform_Type_for_Aboriginal_stone_artefacts", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Platform_Type_for_Aboriginal_stone_artefacts\")");
addOnEvent("Artefact/Artefact/Termination_for_Aboriginal_stone_artefacts", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Termination_for_Aboriginal_stone_artefacts\")");
addOnEvent("Artefact/Artefact/Cross_Section_for_Aboriginal_artefacts", "click", "dropdownItemValue = getFieldValue(\"Artefact/Artefact/Cross_Section_for_Aboriginal_artefacts\")");
addOnEvent("Intangible_Value/Intangible_Value/Intangible_Value_Type", "click", "dropdownItemValue = getFieldValue(\"Intangible_Value/Intangible_Value/Intangible_Value_Type\")");
addOnEvent("Art/Art/Form", "click", "dropdownItemValue = getFieldValue(\"Art/Art/Form\")");
addOnEvent("Art/Art/Main_Colour", "click", "dropdownItemValue = getFieldValue(\"Art/Art/Main_Colour\")");
addOnEvent("Art/Art/Art_Condition", "click", "dropdownItemValue = getFieldValue(\"Art/Art/Art_Condition\")");
addOnEvent("Grinding_Grove/Grinding_Grove/Type_Grinding_Grove", "click", "dropdownItemValue = getFieldValue(\"Grinding_Grove/Grinding_Grove/Type_Grinding_Grove\")");
addOnEvent("Grinding_Grove/Grinding_Grove/Profile_Shape", "click", "dropdownItemValue = getFieldValue(\"Grinding_Grove/Grinding_Grove/Profile_Shape\")");
addOnEvent("Shell/Shell/Distribution", "click", "dropdownItemValue = getFieldValue(\"Shell/Shell/Distribution\")");
addOnEvent("Shell/Shell/Dominant_Species", "click", "dropdownItemValue = getFieldValue(\"Shell/Shell/Dominant_Species\")");
addOnEvent("Shell/Shell/Approximate_percentage_of_dominant_species_as_part_of_assemblage", "click", "dropdownItemValue = getFieldValue(\"Shell/Shell/Approximate_percentage_of_dominant_species_as_part_of_assemblage\")");
addOnEvent("Hearth_Ground_Oven/Hearth_Ground_Oven/Components", "click", "dropdownItemValue = getFieldValue(\"Hearth_Ground_Oven/Hearth_Ground_Oven/Components\")");
addOnEvent("Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Insitu_Potential_Arch_Deposit", "click", "dropdownItemValue = getFieldValue(\"Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Insitu_Potential_Arch_Deposit\")");
addOnEvent("Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Stratified_Arch_Deposit", "click", "dropdownItemValue = getFieldValue(\"Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Stratified_Arch_Deposit\")");
addOnEvent("Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Arch_Deposit_Type", "click", "dropdownItemValue = getFieldValue(\"Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Arch_Deposit_Type\")");
addOnEvent("Culturally_Modified_Tree/Culturally_Modified_Tree/Type_CM_Tree", "click", "dropdownItemValue = getFieldValue(\"Culturally_Modified_Tree/Culturally_Modified_Tree/Type_CM_Tree\")");
addOnEvent("Culturally_Modified_Tree/Culturally_Modified_Tree/Tree_Species", "click", "dropdownItemValue = getFieldValue(\"Culturally_Modified_Tree/Culturally_Modified_Tree/Tree_Species\")");
addOnEvent("Culturally_Modified_Tree/Culturally_Modified_Tree/Living_Status", "click", "dropdownItemValue = getFieldValue(\"Culturally_Modified_Tree/Culturally_Modified_Tree/Living_Status\")");
addOnEvent("Culturally_Modified_Tree/Culturally_Modified_Tree/Tree_Status", "click", "dropdownItemValue = getFieldValue(\"Culturally_Modified_Tree/Culturally_Modified_Tree/Tree_Status\")");
addOnEvent("Culturally_Modified_Tree/Culturally_Modified_Tree/Shape_CM_Tree", "click", "dropdownItemValue = getFieldValue(\"Culturally_Modified_Tree/Culturally_Modified_Tree/Shape_CM_Tree\")");
addOnEvent("Culturally_Modified_Tree/Culturally_Modified_Tree/Orientation", "click", "dropdownItemValue = getFieldValue(\"Culturally_Modified_Tree/Culturally_Modified_Tree/Orientation\")");
addOnEvent("Culturally_Modified_Tree/Culturally_Modified_Tree/Carving_type", "click", "dropdownItemValue = getFieldValue(\"Culturally_Modified_Tree/Culturally_Modified_Tree/Carving_type\")");
addOnEvent("Culturally_Modified_Tree/Culturally_Modified_Tree/Axe_Marks", "click", "dropdownItemValue = getFieldValue(\"Culturally_Modified_Tree/Culturally_Modified_Tree/Axe_Marks\")");

/******************************************************************************/
/*                                 ACTION BAR                                 */
/******************************************************************************/
addActionBarItem("clean_synced_files", new ActionButtonCallback() {
  actionOnLabel() {
    "{Clean_Synced_Files}";
  }
  actionOn() {
    cleanSyncedFiles();
  }
});

addActionBarItem("sync", new ToggleActionButtonCallback() {
  actionOnLabel() {
    "{Disable_Sync}";
  }
  actionOn() {
    setSyncEnabled(false);
    setFileSyncEnabled(false);
    showToast("{Sync_Disabled}");
  }
  isActionOff() {
    isSyncEnabled();
  }
  actionOffLabel() {
    "{Enable_Sync}";
  }
  actionOff() {
    setSyncEnabled(true);
    setFileSyncEnabled(true);
    showToast("{Sync_Enabled}");
  }
});

addActionBarItem("internal_gps", new ToggleActionButtonCallback() {
  actionOnLabel() {
    "{Disable_Internal_GPS}";
  }
  actionOn() {
    stopGPS();
    showToast("{Internal_GPS_Disabled}");
    updateGPSDiagnostics();
  }
  isActionOff() {
    isInternalGPSOn();
  }
  actionOffLabel() {
    "{Enable_Internal_GPS}";
  }
  actionOff() {
    if(isExternalGPSOn()) {
      stopGPS();
    }
    startInternalGPS();
    showToast("{Internal_GPS_Enabled}");
    updateGPSDiagnostics();
  }
});

addActionBarItem("external_gps", new ToggleActionButtonCallback() {
  actionOnLabel() {
    "{Disable_External_GPS}";
  }
  actionOn() {
    stopGPS();
    if (isBluetoothConnected()) {
      showToast("{External_GPS_Disabled}");
    } else {
      showToast("{Please_Enable_Bluetooth}");
    }
    updateGPSDiagnostics();
  }
  isActionOff() {
    isExternalGPSOn();
  }
  actionOffLabel() {
    "{Enable_External_GPS}";
  }
  actionOff() {
    if(isInternalGPSOn()) {
      stopGPS();
    }
    startExternalGPS();
    if(isBluetoothConnected()) {
      showToast("{External_GPS_Enabled}");
    } else {
      showToast("{Please_Enable_Bluetooth}");
      this.actionOn();
    }
    updateGPSDiagnostics();
  }
});


/******************************************************************************/
/*                                    GPS                                     */
/******************************************************************************/

updateGPSDiagnostics() {
  String diagnosticsRef = "";
  if (diagnosticsRef.equals("")) {
    return;
  }

  String status         = "";
  String previousStatus = getFieldValue(diagnosticsRef);
  String notInitialised = "{GPS_is_not_initialised}";

  // Check if GPS is initialised or was previously initialised.
  if (!isExternalGPSOn() && !isInternalGPSOn()) {
    if (!isNull(previousStatus) && !previousStatus.equals(notInitialised)) { // previous gps status is some last valid coordinate.
      // This is hackish. Arch16n substitution happens only at display-time, but the following if clause requires substitution to have happened at run-time
      String error = "";
      error = "{GPS_is_no_longer_initialised}. {Previous_status}:";
      setFieldValue(diagnosticsRef, error);   // Arch16n entry is substituted after this
      error = getFieldValue(diagnosticsRef);

      // check that error message wasn't previously appended to the previous status message.
      if (previousStatus.length()    >= error.length() &&
          previousStatus.subSequence(0, error.length()).equals(error)) {
        status = previousStatus;
      } else {
        status = error + "\n" + previousStatus;
      }
    } else {
      status = notInitialised;
    }
  } else {
    status += "{Internal_GPS}: ";
    if (isInternalGPSOn())
    {
      status += "{on}";
    } else {
      status += "{off}";
    }
    status += "\nExternal GPS: ";
    if (isExternalGPSOn())
    {
      if (isBluetoothConnected()) {
        status += "{on_and_bluetooth_connected}";
      } else {
        status += "{on_and_bluetooth_disconnected}";
      }
    } else {
      status += "{off}";
    }
    Object position = getGPSPosition();
    if (position != null) {
      Object projPosition = getGPSPositionProjected();
      status += "\n{Latitude}: " + position.getLatitude();
      status += "   {Longitude}: " + position.getLongitude();
      status += "\n{Northing}: " + projPosition.getLatitude();
      status += "   {Easting}: " + projPosition.getLongitude();
      status += "\n{Accuracy}: " + getGPSEstimatedAccuracy();
    } else {
      status += "\n{Position}: {no_GPS_position_could_be_found}";
    }
  }
  setFieldValue(diagnosticsRef, status);
}

/******************************************************************************/
/*                                 USER LOGIN                                 */
/******************************************************************************/

String userMenuPath = "User/User/User";

populateListForUsers(){
  String getNonDeletedUsersQuery = "SELECT userid, fname || ' ' || lname "+
                                   "  FROM user "+
                                   " WHERE userdeleted is null;";

  fetchAll(getNonDeletedUsersQuery, new FetchCallback() {
    onFetch(result) {
      populateList(userMenuPath, result);
    }
  });
}

selectUser () {
  String userVocabId  = getListItemValue();
  String userQ        = "SELECT userid,fname,lname,email FROM user " +
                        "WHERE  userid='" + userVocabId + "';";
  FetchCallback callback = new FetchCallback() {
    onFetch(result) {
      user = new User(
            result.get(0),
            result.get(1),
            result.get(2),
            result.get(3)
      );
      setUser(user);
      username = result.get(1) + " " + result.get(2);
    }
  };

  fetchOne(userQ, callback);
}

addOnEvent(userMenuPath, "show",  "populateListForUsers()");
addOnEvent(userMenuPath, "click", "selectUser()");


/******************************************************************************/
/*                              MENU POPULATION                               */
/******************************************************************************/
/** Fetches the contents of a specifed vocabulary and stores it in the given list. **/
fetchVocab(String vocabName, List storageList) {
  fetchVocab(vocabName, storageList, null);
}
fetchVocab(String vocabName, List storageList, String callbackFunction) {
  fetchAll("select vocabid, vocabname from vocabulary left join attributekey using (attributeid) where attributename = '" + vocabName + "';", new FetchCallback() {
    onFetch(result) {
      storageList.addAll(result);
      Log.d("fetchVocab()", "Fetched vocabulary \"" + vocabname + "\" contents: " + result.toString());
      if (callbackFunction != null && !isNull(callbackFunction)) {
        execute(callbackFunction);
      }
    }
  });
}

/** Wrapper for to make a vocab without an exlusion list **/
makeVocab(String type, String path, String attrib) {
  makeVocab(type, path, attrib, null);
}

/** Vocab Population **/
/* Populates the path specified vocabulary from the database based on the given attribute name, where type 
is the type of the vocab to populate (PictureGallery, HierarchicalPictureGallery, CheckBoxGroup, DropDown, HierarchicalDropDown, RadioGroup or List). */
makeVocab(String type, String path, String attrib, List vocabExclusions) {
    makeVocab(type, path, attrib, vocabExclusions, null);
}

/* Populates the path specified vocabulary from the database based on the given attribute name, where type 
is the type of the vocab to populate (PictureGallery, HierarchicalPictureGallery, CheckBoxGroup, DropDown, HierarchicalDropDown, RadioGroup or List). */
makeVocab(String type, String path, String attrib, List vocabExclusions, String callbackFunction){
  if (isNull(type) || isNull(path) || isNull(attrib)) {
    Log.e("makeVocab()", "Can't make populate a vocab when the given type, path or attribute is Null");
    return;
  }

  if (type.equals("PictureGallery")) {
    String pictureGalleryQuery = "SELECT vocabid, vocabname, pictureurl "+
                                 "  FROM vocabulary "+
                                 "  LEFT OUTER JOIN attributekey USING (attributeid) "+
                                 " WHERE attributename = '" + attrib + "' "+
                                 " ORDER BY  vocabcountorder;";
    fetchAll(pictureGalleryQuery, new FetchCallback() {
      onFetch(pictures) {
        populatePictureGallery(path, pictures);
        if (callbackFunction != null && !isNull(callbackFunction)) {
          execute(callbackFunction);
        }
      }
    });
    return;
  }

  if (type.equals("HierarchicalPictureGallery")) {
    populateHierarchicalPictureGallery(path, attrib);
    if (callbackFunction != null && !isNull(callbackFunction)) {
      execute(callbackFunction);
    }
    return;
  }

  if (type.equals("HierarchicalDropDown")) {
    // populateHierarchicalDropDown(path, attrib);
    populateHierarchicalDropDown(path, attrib, true);
    if (callbackFunction != null && !isNull(callbackFunction)) {
      execute(callbackFunction);
    }
    return;
  }

  String getAttributeVocabQuery = "SELECT vocabid, vocabname "+
                                  "  FROM vocabulary "+
                                  "  JOIN attributekey USING (attributeid) "+
                                  " WHERE attributename = '" + attrib + "' "+
                                  " ORDER BY vocabcountorder;";
  fetchAll(getAttributeVocabQuery,
    new FetchCallback() {
      onFetch(result) {
        // print("makeVocab() result: " + result);
        if (result!=null && result.size()>0 && vocabExclusions!=null && vocabExclusions.size()>0) {
          List filteredVocab = new ArrayList();
          for(item : result) {
            if (vocabExclusions.contains(item.get(1))) {
              Log.d("makeVocab()", "removing vocab exclusion: " + item.get(1));
            } else {
              filteredVocab.add(item);
            }
          }
          result=filteredVocab;
        }
        // print("makeVocab() filtered result: " + result);
        if(type.equals("CheckBoxGroup")) {
          populateCheckBoxGroup(path, result);
        } else if(type.equals("DropDown")) {
          // populateDropDown(path, result);
          populateDropDown(path, result, true);
        } else if(type.equals("RadioGroup")) {
          populateRadioGroup(path, result);
        } else if(type.equals("List")) {
          populateList(path, result);
        }
        if (callbackFunction != null && !isNull(callbackFunction)) {
          execute(callbackFunction);
        }
      }
    });
}
makeVocab("RadioGroup", "Transect/Transect/Device_ID", "Device ID");
makeVocab("DropDown", "Transect/Transect/Landform", "Landform");
makeVocab("DropDown", "Transect/Transect/Slope_Gradient", "Slope Gradient");
makeVocab("DropDown", "Transect/Transect/Geomorphological_Activity", "Geomorphological Activity");
makeVocab("RadioGroup", "Site/Site/Open_Closed_Site", "Open Closed Site");
makeVocab("DropDown", "Site/Site/Site_Aspect_Orientation", "Site Aspect Orientation");
makeVocab("CheckBoxGroup", "Site/Site/General_Condition_of_Site", "General Condition of Site");
makeVocab("RadioGroup", "Site/Open_Site/Subsurfance_potential", "Subsurfance potential");
makeVocab("DropDown", "Site/Closed_Site/Shelter_Cave_Formation", "Shelter Cave Formation");
makeVocab("DropDown", "Site/Closed_Site/Rock_Surface_Condition", "Rock Surface Condition");
makeVocab("CheckBoxGroup", "Feature/Feature/Feature_Type", "Feature Type");
makeVocab("DropDown", "Feature/Feature/Feature_Condition", "Feature Condition");
makeVocab("DropDown", "Artefact/Artefact/Insitu_Artefact", "Insitu Artefact");
makeVocab("DropDown", "Artefact/Artefact/Stratified_Artefact", "Stratified Artefact");
makeVocab("DropDown", "Artefact/Artefact/Artefact_Raw_Material", "Artefact Raw Material");
makeVocab("DropDown", "Artefact/Artefact/Artefact_Type", "Artefact Type");
makeVocab("HierarchicalDropDown", "Artefact/Artefact/Artefact_Description_for_Aboriginal_Stone_Artefacts", "Artefact Description for Aboriginal Stone Artefacts");
makeVocab("HierarchicalDropDown", "Artefact/Artefact/Artefact_Colour", "Artefact Colour");
makeVocab("DropDown", "Artefact/Artefact/Artefact_Status_Modification", "Artefact Status Modification");
makeVocab("DropDown", "Artefact/Artefact/Platform_Surface_for_Aboriginal_stone_artefacts", "Platform Surface for Aboriginal stone artefacts");
makeVocab("DropDown", "Artefact/Artefact/Platform_Type_for_Aboriginal_stone_artefacts", "Platform Type for Aboriginal stone artefacts");
makeVocab("DropDown", "Artefact/Artefact/Termination_for_Aboriginal_stone_artefacts", "Termination for Aboriginal stone artefacts");
makeVocab("DropDown", "Artefact/Artefact/Cross_Section_for_Aboriginal_artefacts", "Cross Section for Aboriginal artefacts");
makeVocab("DropDown", "Intangible_Value/Intangible_Value/Intangible_Value_Type", "Intangible Value Type");
makeVocab("RadioGroup", "Art/Art/Pigment_Engraved", "Pigment Engraved");
makeVocab("RadioGroup", "Art/Art/Super-impositioning", "Super-impositioning");
makeVocab("CheckBoxGroup", "Art/Art/Motif_s", "Motif s");
makeVocab("CheckBoxGroup", "Art/Art/Application_Technique", "Application Technique");
makeVocab("DropDown", "Art/Art/Form", "Form");
makeVocab("DropDown", "Art/Art/Main_Colour", "Main Colour");
makeVocab("CheckBoxGroup", "Art/Art/Art_Location", "Art Location");
makeVocab("DropDown", "Art/Art/Art_Condition", "Art Condition");
makeVocab("DropDown", "Grinding_Grove/Grinding_Grove/Type_Grinding_Grove", "Type Grinding Grove");
makeVocab("DropDown", "Grinding_Grove/Grinding_Grove/Profile_Shape", "Profile Shape");
makeVocab("DropDown", "Shell/Shell/Distribution", "Distribution");
makeVocab("CheckBoxGroup", "Shell/Shell/Species_present", "Species present");
makeVocab("DropDown", "Shell/Shell/Dominant_Species", "Dominant Species");
makeVocab("DropDown", "Shell/Shell/Approximate_percentage_of_dominant_species_as_part_of_assemblage", "Approximate percentage of dominant species as part of assemblage");
makeVocab("DropDown", "Hearth_Ground_Oven/Hearth_Ground_Oven/Components", "Components");
makeVocab("DropDown", "Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Insitu_Potential_Arch_Deposit", "Insitu Potential Arch Deposit");
makeVocab("DropDown", "Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Stratified_Arch_Deposit", "Stratified Arch Deposit");
makeVocab("DropDown", "Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Arch_Deposit_Type", "Arch Deposit Type");
makeVocab("RadioGroup", "Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Surface_Artefacts", "Surface Artefacts");
makeVocab("DropDown", "Culturally_Modified_Tree/Culturally_Modified_Tree/Type_CM_Tree", "Type CM Tree");
makeVocab("DropDown", "Culturally_Modified_Tree/Culturally_Modified_Tree/Tree_Species", "Tree Species");
makeVocab("DropDown", "Culturally_Modified_Tree/Culturally_Modified_Tree/Living_Status", "Living Status");
makeVocab("DropDown", "Culturally_Modified_Tree/Culturally_Modified_Tree/Tree_Status", "Tree Status");
makeVocab("CheckBoxGroup", "Culturally_Modified_Tree/Culturally_Modified_Tree/Damage", "Damage");
makeVocab("RadioGroup", "Culturally_Modified_Tree/Culturally_Modified_Tree/Regrowth", "Regrowth");
makeVocab("RadioGroup", "Culturally_Modified_Tree/Culturally_Modified_Tree/Is_a_tree_health_assessment_required", "Is a tree health assessment required");
makeVocab("DropDown", "Culturally_Modified_Tree/Culturally_Modified_Tree/Shape_CM_Tree", "Shape CM Tree");
makeVocab("DropDown", "Culturally_Modified_Tree/Culturally_Modified_Tree/Orientation", "Orientation");
makeVocab("DropDown", "Culturally_Modified_Tree/Culturally_Modified_Tree/Carving_type", "Carving type");
makeVocab("DropDown", "Culturally_Modified_Tree/Culturally_Modified_Tree/Axe_Marks", "Axe Marks");


/******************************************************************************/
/*                                 VALIDATION                                 */
/******************************************************************************/
/* `ref`  is a reference/path to a field
 * `name` is a human-readable name for that field
 * `cond` is a String containing a boolean expression that evaluates to true if
 *        and only if the the field pair returned by this function should be
 *        validated.
 *
 *  Returns a field pair (really just an ArrayList).
 */
fieldPair(String ref, String name, String cond) {
  List fp = new ArrayList();
  fp.add(ref);
  fp.add(name);
  fp.add(cond);
  return fp;
}

fieldPair(String ref, String name) {
  String t = "true";
  return fieldPair(ref, name, t);
}

/* Returns true if field specified by `ref` is valid. False otherwise.
 */
isValidField(String ref) {
  return !isNull(getFieldValue(ref));
}
/* `format` can either be HTML or PLAINTEXT
 */
validateFields(List fields, String format) {
  Integer numInvalid = 0;

  /* Build validation message string (and count how many invalid fields exist) */
  String out = "Please fill out the following fields:\n";
  for(f : fields) {
    String ref  = f.get(0); // Reference to field
    String name = f.get(1); // Human-readable name
    String cond = f.get(2); // Validation condition

    // Only validate a field whose validation condition evaluates to `true`
    Boolean doValidateField = (Boolean) eval(cond);
    if (!doValidateField)
      continue;

    // Add any invalid fields to the output and tally them
    if (!isValidField(ref)) {
      out += "- " + name + "\n";
      numInvalid++;
    }
  }
  // All the fields are valid; just overwrite `out` with a cheery message
  if (numInvalid == 0)
    out = "All fields contain valid data!";

  /* Format the output as dictated by `format` */
  if (format == "HTML") {
    out = out.replace("\n", "<br>");
  } else if (format == "PLAINTEXT") {
    ;
  }

  return out;
}

validateTransect() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Transect/Transect/Transect_ID", "{Transect_ID}"));
  f.add(fieldPair("Transect/Transect/Device_ID", "{Device_ID}"));
  f.add(fieldPair("Transect/Transect/Number_of_Participants_in_Transect", "{Number_of_Participants_in_Transect}"));
  f.add(fieldPair("Transect/Transect/Landform", "{Landform}"));
  f.add(fieldPair("Transect/Transect/Slope_Gradient", "{Slope_Gradient}"));
  f.add(fieldPair("Transect/Transect/Vegetation_notes", "{Vegetation_notes}"));
  f.add(fieldPair("Transect/Transect/Ground_surface_visibility", "{Ground_surface_visibility}"));
  f.add(fieldPair("Transect/Transect/Ground_surface_exposure", "{Ground_surface_exposure}"));
  f.add(fieldPair("Transect/Transect/Geomorphological_Activity", "{Geomorphological_Activity}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateSite() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Site/Site/Site_ID_Number", "{Site_ID_Number}"));
  f.add(fieldPair("Site/Site/General_Condition_of_Site", "{General_Condition_of_Site}"));
  f.add(fieldPair("Site/Open_Site/Total_length_of_visible_site", "{Total_length_of_visible_site}"));
  f.add(fieldPair("Site/Open_Site/Average_width_of_visible_site", "{Average_width_of_visible_site}"));
  f.add(fieldPair("Site/Open_Site/Estimated_area_of_visible_site", "{Estimated_area_of_visible_site}"));
  f.add(fieldPair("Site/Open_Site/Length_of_assessed_site_area", "{Length_of_assessed_site_area}"));
  f.add(fieldPair("Site/Open_Site/Average_width_of_potential_site_area", "{Average_width_of_potential_site_area}"));
  f.add(fieldPair("Site/Closed_Site/Internal_Length", "{Internal_Length}"));
  f.add(fieldPair("Site/Closed_Site/Internal_Width", "{Internal_Width}"));
  f.add(fieldPair("Site/Closed_Site/Shelter_Height", "{Shelter_Height}"));
  f.add(fieldPair("Site/Closed_Site/Shelter_Floor_Area", "{Shelter_Floor_Area}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateFeature() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Feature/Feature/Feature_ID", "{Feature_ID}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateArtefact() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Artefact/Artefact/Instance_No", "{Instance_No_}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateIntangibleValue() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Intangible_Value/Intangible_Value/Intangible_Value_ID", "{Intangible_Value_ID}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateArt() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Art/Art/Art_ID", "{Art_ID}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateOtherFeature() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Other_Feature/Other_Feature/Other_Feature_ID", "{Other_Feature_ID}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateGrindingGrove() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Grinding_Grove/Grinding_Grove/Grinding_Grove_ID", "{Grinding_Grove_ID}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateShell() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Shell/Shell/Shell_ID", "{Shell_ID}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateHearthGroundOven() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Hearth_Ground_Oven/Hearth_Ground_Oven/Hearth_ID", "{Hearth_ID}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validatePotentialArchaeologicalDeposit() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Arch_Dep_ID", "{Arch_Dep_ID}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateCulturallyModifiedTree() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Culturally_Modified_Tree/Culturally_Modified_Tree/CM_Tree_ID", "{CM_Tree_ID}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}

validateCulturalManagementComment() {
  List f = new ArrayList(); // Fields to be validated

  f.add(fieldPair("Cultural_Management_Comment/C_M_Comments/Comment_ID", "{Comment_ID}"));

  String validationMessage = validateFields(f, "PLAINTEXT");
  showWarning("Validation Results", validationMessage);
}



/******************************************************************************/
/*                                 AUTOSAVING                                 */
/******************************************************************************/
Map tabgroupToUuid = new HashMap();

getUuid(String tabgroup) {
  tabgroupToUuid.get(tabgroup);
}

setUuid(String tabgroup, String uuid) {
  tabgroupToUuid.put(tabgroup, uuid);
}

saveTabGroup(String tabgroup) {
  saveTabGroup(tabgroup, "");
}

saveTabGroup(String tabgroup, String callback) {
  Boolean enableAutosave  = true;
  String  id              = getUuid(tabgroup);
  List    geometry        = null;
  List    attributes      = null;
  String  parentTabgroup_ = parentTabgroup;
  Boolean userWasSet      = !username.equals("");

  parentTabgroup = null;
  SaveCallback saveCallback  = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      // Make a child-parent relationship if need be.
      if (newRecord && !isNull(parentTabgroup_)) {
        String rel = "";
        rel += parentTabgroup_.replaceAll("_", " ");
        rel += " - ";
        rel += tabgroup.replaceAll("_", " ");
        saveEntitiesToHierRel(
          rel,
          getUuid(parentTabgroup_),
          uuid,
          "Parent Of",
          "Child Of",
          null
        );
      }

      // This fixes an interesting bug. Without this, if a user was not set
      // (by calling `setUser`) at the time `saveTabGroup` was first called, but
      // set by the time `onSave` was called, the tab group is saved correctly
      // the first time only.
      //
      // Adding this allows subsequent saves to succeed. Presumably it plays
      // some role in helping FAIMS associate the correct user with a record.
      if (!userWasSet) {
        saveTabGroup(tabgroup);
      }

      execute(callback);
    }
    onError(message) {
      showToast(message);
    }
  };

  populateAuthorAndTimestamp(tabgroup);
  saveTabGroup(tabgroup, id, geometry, attributes, saveCallback, enableAutosave);
}

populateAuthorAndTimestamp(String tabgroup) {
  Map tabgroupToAuthor    = new HashMap();
  Map tabgroupToTimestamp = new HashMap();
  tabgroupToAuthor.put("Transect", "Transect/Transect/Author");
  tabgroupToTimestamp.put("Transect", "Transect/Transect/Timestamp");

  String uuid          = getUuid(tabgroup);
  String authorPath    = tabgroupToAuthor.get(tabgroup);
  String timestampPath = tabgroupToTimestamp.get(tabgroup);
  if (isNull(uuid)) {
    if (!isNull(authorPath))
      setFieldValue(authorPath,    "Entity not yet saved");
    if (!isNull(timestampPath))
      setFieldValue(timestampPath, "Entity not yet saved");
    return;
  }

  String q = "SELECT createdat, createdby " +
             "  FROM createdmodifiedatby " +
             " WHERE uuid = '" + uuid + "'";
  FetchCallback callback = new FetchCallback() {
    onFetch(result) {
      if (!isNull(timestampPath))
        setFieldValue(timestampPath, result.get(0));
      if (!isNull(authorPath))
        setFieldValue(authorPath,    result.get(1));
    }
  };

  fetchOne(q, callback);
}

onShowTransect () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Transect");
}
onShowSite () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Site");
}
onShowFeature () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Feature");
}
onShowArtefact () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Artefact");
}
onShowIntangibleValue () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Intangible_Value");
}
onShowArt () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Art");
}
onShowOtherFeature () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Other_Feature");
}
onShowGrindingGrove () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Grinding_Grove");
}
onShowShell () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Shell");
}
onShowHearthGroundOven () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Hearth_Ground_Oven");
}
onShowPotentialArchaeologicalDeposit () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Potential_Archaeological_Deposit");
}
onShowCulturallyModifiedTree () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Culturally_Modified_Tree");
}
onShowCulturalManagementComment () {
  // TODO: Add some things which should happen when this tabgroup is shown
  saveTabGroup("Cultural_Management_Comment");
}

addOnEvent("Transect", "show", "onShowTransect()");
addOnEvent("Site", "show", "onShowSite()");
addOnEvent("Feature", "show", "onShowFeature()");
addOnEvent("Artefact", "show", "onShowArtefact()");
addOnEvent("Intangible_Value", "show", "onShowIntangibleValue()");
addOnEvent("Art", "show", "onShowArt()");
addOnEvent("Other_Feature", "show", "onShowOtherFeature()");
addOnEvent("Grinding_Grove", "show", "onShowGrindingGrove()");
addOnEvent("Shell", "show", "onShowShell()");
addOnEvent("Hearth_Ground_Oven", "show", "onShowHearthGroundOven()");
addOnEvent("Potential_Archaeological_Deposit", "show", "onShowPotentialArchaeologicalDeposit()");
addOnEvent("Culturally_Modified_Tree", "show", "onShowCulturallyModifiedTree()");
addOnEvent("Cultural_Management_Comment", "show", "onShowCulturalManagementComment()");

onClickUserUser () {
  // TODO: Add some things which should happen when this element is clicked
  newTab("Control", true);
}
onClickControlNewTransect () {
  // TODO: Add some things which should happen when this element is clicked
  newTransect();
}
onClickControlNewSite () {
  // TODO: Add some things which should happen when this element is clicked
  newSite();
}
onClickControlNewFeature () {
  // TODO: Add some things which should happen when this element is clicked
  newFeature();
}
onClickControlNewArtefact () {
  // TODO: Add some things which should happen when this element is clicked
  newArtefact();
}
onClickControlNewIntangibleValue () {
  // TODO: Add some things which should happen when this element is clicked
  newIntangibleValue();
}
onClickControlNewArt () {
  // TODO: Add some things which should happen when this element is clicked
  newArt();
}
onClickControlNewOtherFeature () {
  // TODO: Add some things which should happen when this element is clicked
  newOtherFeature();
}
onClickControlNewGrindingGrove () {
  // TODO: Add some things which should happen when this element is clicked
  newGrindingGrove();
}
onClickControlNewShell () {
  // TODO: Add some things which should happen when this element is clicked
  newShell();
}
onClickControlNewHearthGroundOven () {
  // TODO: Add some things which should happen when this element is clicked
  newHearthGroundOven();
}
onClickControlNewPotentialArchaeologicalDeposit () {
  // TODO: Add some things which should happen when this element is clicked
  newPotentialArchaeologicalDeposit();
}
onClickControlNewCulturallyModifiedTree () {
  // TODO: Add some things which should happen when this element is clicked
  newCulturallyModifiedTree();
}
onClickControlNewCulturalManagementComment () {
  // TODO: Add some things which should happen when this element is clicked
  newCulturalManagementComment();
}
onClickSiteAddFeature () {
  // TODO: Add some things which should happen when this element is clicked
  String tabgroup = "Site";
  if (isNull(getUuid(tabgroup))){
    showToast("{You_must_save_this_tabgroup_first}");
    return;
  }
  parentTabgroup = tabgroup;
  newFeature();
}
onClickFeatureAddArtefact () {
  // TODO: Add some things which should happen when this element is clicked
  String tabgroup = "Feature";
  if (isNull(getUuid(tabgroup))){
    showToast("{You_must_save_this_tabgroup_first}");
    return;
  }
  parentTabgroup = tabgroup;
  newArtefact();
}
onClickFeatureAddIntangibleValue () {
  // TODO: Add some things which should happen when this element is clicked
  String tabgroup = "Feature";
  if (isNull(getUuid(tabgroup))){
    showToast("{You_must_save_this_tabgroup_first}");
    return;
  }
  parentTabgroup = tabgroup;
  newIntangibleValue();
}
onClickFeatureAddArt () {
  // TODO: Add some things which should happen when this element is clicked
  String tabgroup = "Feature";
  if (isNull(getUuid(tabgroup))){
    showToast("{You_must_save_this_tabgroup_first}");
    return;
  }
  parentTabgroup = tabgroup;
  newArt();
}
onClickFeatureAddOtherFeature () {
  // TODO: Add some things which should happen when this element is clicked
  String tabgroup = "Feature";
  if (isNull(getUuid(tabgroup))){
    showToast("{You_must_save_this_tabgroup_first}");
    return;
  }
  parentTabgroup = tabgroup;
  newOtherFeature();
}
onClickFeatureAddGrindingGrove () {
  // TODO: Add some things which should happen when this element is clicked
  String tabgroup = "Feature";
  if (isNull(getUuid(tabgroup))){
    showToast("{You_must_save_this_tabgroup_first}");
    return;
  }
  parentTabgroup = tabgroup;
  newGrindingGrove();
}
onClickFeatureAddShell () {
  // TODO: Add some things which should happen when this element is clicked
  String tabgroup = "Feature";
  if (isNull(getUuid(tabgroup))){
    showToast("{You_must_save_this_tabgroup_first}");
    return;
  }
  parentTabgroup = tabgroup;
  newShell();
}
onClickFeatureAddHearthGroundOven () {
  // TODO: Add some things which should happen when this element is clicked
  String tabgroup = "Feature";
  if (isNull(getUuid(tabgroup))){
    showToast("{You_must_save_this_tabgroup_first}");
    return;
  }
  parentTabgroup = tabgroup;
  newHearthGroundOven();
}
onClickFeatureAddPotentialArchaeologicalDeposit () {
  // TODO: Add some things which should happen when this element is clicked
  String tabgroup = "Feature";
  if (isNull(getUuid(tabgroup))){
    showToast("{You_must_save_this_tabgroup_first}");
    return;
  }
  parentTabgroup = tabgroup;
  newPotentialArchaeologicalDeposit();
}
onClickFeatureAddCulturallyModifiedTree () {
  // TODO: Add some things which should happen when this element is clicked
  String tabgroup = "Feature";
  if (isNull(getUuid(tabgroup))){
    showToast("{You_must_save_this_tabgroup_first}");
    return;
  }
  parentTabgroup = tabgroup;
  newCulturallyModifiedTree();
}

addOnEvent("User/User/User", "click", "onClickUserUser()");
addOnEvent("Control/Control/New_Transect", "click", "onClickControlNewTransect()");
addOnEvent("Control/Control/New_Site", "click", "onClickControlNewSite()");
addOnEvent("Control/Control/New_Feature", "click", "onClickControlNewFeature()");
addOnEvent("Control/Control/New_Artefact", "click", "onClickControlNewArtefact()");
addOnEvent("Control/Control/New_Intangible_Value", "click", "onClickControlNewIntangibleValue()");
addOnEvent("Control/Control/New_Art", "click", "onClickControlNewArt()");
addOnEvent("Control/Control/New_Other_Feature", "click", "onClickControlNewOtherFeature()");
addOnEvent("Control/Control/New_Grinding_Grove", "click", "onClickControlNewGrindingGrove()");
addOnEvent("Control/Control/New_Shell", "click", "onClickControlNewShell()");
addOnEvent("Control/Control/New_Hearth_Ground_Oven", "click", "onClickControlNewHearthGroundOven()");
addOnEvent("Control/Control/New_Potential_Archaeological_Deposit", "click", "onClickControlNewPotentialArchaeologicalDeposit()");
addOnEvent("Control/Control/New_Culturally_Modified_Tree", "click", "onClickControlNewCulturallyModifiedTree()");
addOnEvent("Control/Control/New_Cultural_Management_Comment", "click", "onClickControlNewCulturalManagementComment()");
addOnEvent("Site/Features/Add_Feature", "click", "onClickSiteAddFeature()");
addOnEvent("Feature/Artefact/Add_Artefact", "click", "onClickFeatureAddArtefact()");
addOnEvent("Feature/Intangible_Value/Add_Intangible_Value", "click", "onClickFeatureAddIntangibleValue()");
addOnEvent("Feature/Art/Add_Art", "click", "onClickFeatureAddArt()");
addOnEvent("Feature/Other_Feature/Add_Other_Feature", "click", "onClickFeatureAddOtherFeature()");
addOnEvent("Feature/Grinding_Grove/Add_Grinding_Grove", "click", "onClickFeatureAddGrindingGrove()");
addOnEvent("Feature/Shell/Add_Shell", "click", "onClickFeatureAddShell()");
addOnEvent("Feature/Hearth_Ground_Oven/Add_Hearth_Ground_Oven", "click", "onClickFeatureAddHearthGroundOven()");
addOnEvent("Feature/Potential_Archaeological_Deposit/Add_Potential_Archaeological_Deposit", "click", "onClickFeatureAddPotentialArchaeologicalDeposit()");
addOnEvent("Feature/Culturally_Modified_Tree/Add_Culturally_Modified_Tree", "click", "onClickFeatureAddCulturallyModifiedTree()");


/******************************************************************************/
/*                   AUDIO, CAMERA, FILE AND VIDEO BINDINGS                   */
/******************************************************************************/
addOnEvent("Transect/Transect/Button_Photo_Transect", "click", "attachPictureTo(\"Transect/Transect/Photo_Transect\")");
addOnEvent("Artefact/Artefact/Button_Photo_Artefact", "click", "attachPictureTo(\"Artefact/Artefact/Photo_Artefact\")");
addOnEvent("Art/Art/Button_Photo_Art", "click", "attachPictureTo(\"Art/Art/Photo_Art\")");
addOnEvent("Other_Feature/Other_Feature/Button_Photo_Feature", "click", "attachPictureTo(\"Other_Feature/Other_Feature/Photo_Feature\")");
addOnEvent("Grinding_Grove/Grinding_Grove/Button_Photo_Grinding_Grove", "click", "attachPictureTo(\"Grinding_Grove/Grinding_Grove/Photo_Grinding_Grove\")");
addOnEvent("Shell/Shell/Button_Photo_Shell", "click", "attachPictureTo(\"Shell/Shell/Photo_Shell\")");
addOnEvent("Hearth_Ground_Oven/Hearth_Ground_Oven/Button_Photo_Hearth", "click", "attachPictureTo(\"Hearth_Ground_Oven/Hearth_Ground_Oven/Photo_Hearth\")");
addOnEvent("Transect/Transect/Button_Sketch_Transect", "click", "attachFileTo(\"Transect/Transect/Sketch_Transect\")");
addOnEvent("Art/Art/Button_Sketch_Plan_Art", "click", "attachFileTo(\"Art/Art/Sketch_Plan_Art\")");
addOnEvent("Other_Feature/Other_Feature/Button_Sketch_Plan_Feature", "click", "attachFileTo(\"Other_Feature/Other_Feature/Sketch_Plan_Feature\")");
addOnEvent("Grinding_Grove/Grinding_Grove/Button_Sketch_Plan_Grinding_Grove", "click", "attachFileTo(\"Grinding_Grove/Grinding_Grove/Sketch_Plan_Grinding_Grove\")");
addOnEvent("Shell/Shell/Button_Sketch_Plan_Shell", "click", "attachFileTo(\"Shell/Shell/Sketch_Plan_Shell\")");
addOnEvent("Hearth_Ground_Oven/Hearth_Ground_Oven/Button_Sketch_Plan_Hearth", "click", "attachFileTo(\"Hearth_Ground_Oven/Hearth_Ground_Oven/Sketch_Plan_Hearth\")");

/******************************************************************************/
/*                             NAVIGATION DRAWER                              */
/******************************************************************************/
removeNavigationButtons() {
  removeNavigationButton("new");
  removeNavigationButton("duplicate");
  removeNavigationButton("delete");
  removeNavigationButton("validate");
}

addNavigationButtons(String tabgroup) {
  removeNavigationButtons();
  List tabgroupsToValidate = new ArrayList();
  tabgroupsToValidate.add("Transect");
  tabgroupsToValidate.add("Site");
  tabgroupsToValidate.add("Feature");
  tabgroupsToValidate.add("Artefact");
  tabgroupsToValidate.add("Intangible_Value");
  tabgroupsToValidate.add("Art");
  tabgroupsToValidate.add("Other_Feature");
  tabgroupsToValidate.add("Grinding_Grove");
  tabgroupsToValidate.add("Shell");
  tabgroupsToValidate.add("Hearth_Ground_Oven");
  tabgroupsToValidate.add("Potential_Archaeological_Deposit");
  tabgroupsToValidate.add("Culturally_Modified_Tree");
  tabgroupsToValidate.add("Cultural_Management_Comment");

  addNavigationButton("new", new ActionButtonCallback() {
    actionOnLabel() {
      "{New}";
    }
    actionOn() {
      if(!isNull(getUuid(tabgroup))) {
          newRecord(tabgroup);
          showToast("{New_record_created}");
      } else {
          showAlert("{Warning}", "{Any_unsaved_changes_will_be_lost}", "newRecord(\""+tabgroup+"\")", "");
      }
    }
  }, "success");
  addNavigationButton("duplicate", new ActionButtonCallback() {
    actionOnLabel() {
      "{Duplicate}";
    }
    actionOn() {
      if(!isNull(getUuid(tabgroup))) {
          duplicateRecord(tabgroup);
      } else {
          showWarning("{Warning}", "{This_record_is_unsaved_and_cannot_be_duplicated}");
      }
    }
  }, "primary");
  addNavigationButton("delete", new ActionButtonCallback() {
    actionOnLabel() {
      "{Delete}";
    }
    actionOn() {
      deleteRecord(tabgroup);
    }
  }, "danger");
  if (tabgroupsToValidate.contains(tabgroup)) {
    addNavigationButton("validate", new ActionButtonCallback() {
      actionOnLabel() {
        "{Validate}";
      }
      actionOn() {
        String validationFunction = "validate" + tabgroup.replaceAll("_", "") + "()";
        eval(validationFunction);
      }
    }, "default");
  }
}

/******************************************************************************/
/*        ENTITY AND RELATIONSHIP SAVING AND LOADING HELPER FUNCTIONS         */
/******************************************************************************/
/** Saves two entity id's as a relation. **/
saveEntitiesToRel(String type, String entity1, String entity2) {
  String callback = null;
  saveEntitiesToRel(type, entity1, entity2, callback);
}

/** Saves two entity id's as a relation with some callback executed. **/
saveEntitiesToRel(String type, String entity1, String entity2, String callback) {
  String e1verb = null;
  String e2verb = null;
  saveEntitiesToHierRel(type, entity1, entity2, e1verb, e2verb, callback);
}

/** Saves two entity id's as a hierachical relation with some callback executed. **/
saveEntitiesToHierRel(String type, String entity1, String entity2, String e1verb, String e2verb, String callback) {
  if (isNull(entity1) || isNull(entity2)) return;
  saveRel(null, type, null, null, new SaveCallback() {
    onSave(rel_id, newRecord) {
      addReln(entity1, rel_id, e1verb);
      addReln(entity2, rel_id, e2verb);
      if(!isNull(callback)) {
         execute(callback);
      }
    }
    onError(message) {
      Log.e("saveEntitiesToHierRel", message);
      showToast(message);
    }
  });
}

// Makes a new record of the given tabgroup
newRecord(String tabgroup) {
  cancelTabGroup(tabgroup, false);

  String newTabGroupFunction = "new" + tabgroup.replaceAll("_", "") + "()"; // Typical value: "newTabgroup()"
  eval(newTabGroupFunction);

  Log.d("newRecord", tabgroup);
}

// Deletes the current record of the given tabgroup
deleteRecord(String tabgroup) {
  String deleteTabGroupFunction = "delete" + tabgroup.replaceAll("_", "") + "()"; // Typical value: "deleteTabgroup()"
  eval(deleteTabGroupFunction);

  Log.d("deleteRecord", tabgroup);
}

// Duplicates the current record of the given tabgroup
duplicateRecord(String tabgroup) {
  dialog = showBusy("Duplicating", "Please wait...");

  String duplicateTabGroupFunction = "duplicate" + tabgroup.replaceAll("_", "") + "()"; // Typical value: "duplicateTabgroup()"
  eval(duplicateTabGroupFunction);

  Log.d("duplicateRecord", tabgroup);
}

// generic fetch saved attributes query
getDuplicateAttributeQuery(String originalRecordID, String attributesToDupe) {
  if (attributesToDupe.equals("")) {
    attributesToDupe = "''";
  }
  String duplicateQuery = "SELECT attributename, freetext, vocabid, measure, certainty " +
                          "  FROM latestnondeletedaentvalue JOIN attributekey USING (attributeid) " +
                          " WHERE attributename IN ('', "+attributesToDupe+") " +
                          "   AND uuid = '"+originalRecordID+"'; ";
  return duplicateQuery;
}

getDuplicateRelnQuery(String originalRecordID) {
  String dupeRelnQuery = "SELECT relntypename, parentparticipatesverb, childparticipatesverb, parentuuid "+
                         "  FROM parentchild join relationship using (relationshipid) "+
                         "  JOIN relntype using (relntypeid) "+
                         " WHERE childuuid = '"+originalRecordID+"' " +
                         "   AND parentparticipatesverb = 'Parent Of' ";
  return dupeRelnQuery;
}

makeDuplicateRelationships(fetchedAttributes, String newuuid){
  Log.e("Module", "makeDuplicateRelationships");
  for (savedAttribute : fetchedAttributes){
    //  saveEntitiesToHierRel(relnname, parent, child, parentverb, childverb, relSaveCallback);
    //relntypename, parentparticipatesverb, childparticipatesverb, childuuid
    String relntypename           = savedAttribute.get(0);
    String parentparticipatesverb = savedAttribute.get(1);
    String childparticipatesverb  = savedAttribute.get(2);
    String childuuid              = savedAttribute.get(3);
    saveEntitiesToHierRel(relntypename, newuuid, childuuid, parentparticipatesverb, childparticipatesverb, null);
  }
}

// generic get extra attributes
getExtraAttributes(fetchedAttributes) {
  List extraAttributes = createAttributeList();
  Log.d("Module", "Duplicating fetched attributes: " + fetchedAttributes.toString());
  for (savedAttribute : fetchedAttributes) {
    extraAttributes.add(
      createEntityAttribute(
        savedAttribute.get(0),
        savedAttribute.get(1),
        savedAttribute.get(2),
        savedAttribute.get(3),
        savedAttribute.get(4)
      )
    );
  }
  return extraAttributes;
}

loadEntity() {
  loadEntity(false);
}
loadEntity(Boolean isDropdown) {
  if (isDropdown) {
    loadEntityFrom(getDropdownItemValue());
  } else {
    loadEntityFrom(getListItemValue());
  }
}

loadEntityFrom(String entityID) {
  if (isNull(entityID)) {
    Log.e("Module", "Cannot load an entity with a null ID.");
    return;
  }

  String getEntTypeNameQ = "SELECT aenttypename " +
                           "  FROM latestnondeletedarchent " +
                           "  JOIN aenttype " +
                           " USING (aenttypeid) " +
                           " WHERE uuid = '" + entityID + "'";
  fetchAll(getEntTypeNameQ, new FetchCallback() {
    onFetch(result) {
      String archEntName = result.get(0).get(0);
      String loadFunction = "load" + archEntName.replaceAll(" ", "") + "From(entityID)"; // Typical value: loadContextFrom(entityID)
      eval(loadFunction);
    }
  });
}

newTransect(){
  String tabgroup = "Transect";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Transect_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Transect/Transect/Transect_ID";
  incAutoNum(autoNumDest);
}

duplicateTransect(){
  String tabgroup = "Transect";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Transect/Transect/Transect_ID";
  incAutoNum(autoNumDest);

  populateCameraPictureGallery("Transect/Transect/Photo_Transect", new ArrayList());
  populateFileList("Transect/Transect/Sketch_Transect", new ArrayList());


  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();
      excludeAttributes.add("Photo Transect");
      excludeAttributes.add("Sketch Transect");

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteTransect(){
  String tabgroup = "Transect";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteTransect()", "doNotDelete()");
  }
}

reallyDeleteTransect(){
  String tabgroup = "Transect";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newSite(){
  String tabgroup = "Site";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Site_ID_Number");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Site/Site/Site_ID_Number";
  incAutoNum(autoNumDest);
}

duplicateSite(){
  String tabgroup = "Site";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Site/Site/Site_ID_Number";
  incAutoNum(autoNumDest);



  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteSite(){
  String tabgroup = "Site";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteSite()", "doNotDelete()");
  }
}

reallyDeleteSite(){
  String tabgroup = "Site";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newFeature(){
  String tabgroup = "Feature";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Feature_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Feature/Feature/Feature_ID";
  incAutoNum(autoNumDest);
}

duplicateFeature(){
  String tabgroup = "Feature";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Feature/Feature/Feature_ID";
  incAutoNum(autoNumDest);



  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteFeature(){
  String tabgroup = "Feature";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteFeature()", "doNotDelete()");
  }
}

reallyDeleteFeature(){
  String tabgroup = "Feature";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newArtefact(){
  String tabgroup = "Artefact";


  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

}

duplicateArtefact(){
  String tabgroup = "Artefact";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);


  populateCameraPictureGallery("Artefact/Artefact/Photo_Artefact", new ArrayList());


  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();
      excludeAttributes.add("Photo Artefact");

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteArtefact(){
  String tabgroup = "Artefact";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteArtefact()", "doNotDelete()");
  }
}

reallyDeleteArtefact(){
  String tabgroup = "Artefact";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newIntangibleValue(){
  String tabgroup = "Intangible_Value";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Intangible_Value_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Intangible_Value/Intangible_Value/Intangible_Value_ID";
  incAutoNum(autoNumDest);
}

duplicateIntangibleValue(){
  String tabgroup = "Intangible_Value";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Intangible_Value/Intangible_Value/Intangible_Value_ID";
  incAutoNum(autoNumDest);



  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteIntangibleValue(){
  String tabgroup = "Intangible_Value";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteIntangibleValue()", "doNotDelete()");
  }
}

reallyDeleteIntangibleValue(){
  String tabgroup = "Intangible_Value";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newArt(){
  String tabgroup = "Art";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Art_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Art/Art/Art_ID";
  incAutoNum(autoNumDest);
}

duplicateArt(){
  String tabgroup = "Art";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Art/Art/Art_ID";
  incAutoNum(autoNumDest);

  populateFileList("Art/Art/Sketch_Plan_Art", new ArrayList());
  populateCameraPictureGallery("Art/Art/Photo_Art", new ArrayList());


  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();
      excludeAttributes.add("Sketch Plan Art");
      excludeAttributes.add("Photo Art");

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteArt(){
  String tabgroup = "Art";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteArt()", "doNotDelete()");
  }
}

reallyDeleteArt(){
  String tabgroup = "Art";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newOtherFeature(){
  String tabgroup = "Other_Feature";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Other_Feature_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Other_Feature/Other_Feature/Other_Feature_ID";
  incAutoNum(autoNumDest);
}

duplicateOtherFeature(){
  String tabgroup = "Other_Feature";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Other_Feature/Other_Feature/Other_Feature_ID";
  incAutoNum(autoNumDest);

  populateFileList("Other_Feature/Other_Feature/Sketch_Plan_Feature", new ArrayList());
  populateCameraPictureGallery("Other_Feature/Other_Feature/Photo_Feature", new ArrayList());


  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();
      excludeAttributes.add("Sketch Plan Feature");
      excludeAttributes.add("Photo Feature");

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteOtherFeature(){
  String tabgroup = "Other_Feature";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteOtherFeature()", "doNotDelete()");
  }
}

reallyDeleteOtherFeature(){
  String tabgroup = "Other_Feature";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newGrindingGrove(){
  String tabgroup = "Grinding_Grove";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Grinding_Grove_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Grinding_Grove/Grinding_Grove/Grinding_Grove_ID";
  incAutoNum(autoNumDest);
}

duplicateGrindingGrove(){
  String tabgroup = "Grinding_Grove";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Grinding_Grove/Grinding_Grove/Grinding_Grove_ID";
  incAutoNum(autoNumDest);

  populateFileList("Grinding_Grove/Grinding_Grove/Sketch_Plan_Grinding_Grove", new ArrayList());
  populateCameraPictureGallery("Grinding_Grove/Grinding_Grove/Photo_Grinding_Grove", new ArrayList());


  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();
      excludeAttributes.add("Sketch Plan Grinding Grove");
      excludeAttributes.add("Photo Grinding Grove");

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteGrindingGrove(){
  String tabgroup = "Grinding_Grove";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteGrindingGrove()", "doNotDelete()");
  }
}

reallyDeleteGrindingGrove(){
  String tabgroup = "Grinding_Grove";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newShell(){
  String tabgroup = "Shell";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Shell_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Shell/Shell/Shell_ID";
  incAutoNum(autoNumDest);
}

duplicateShell(){
  String tabgroup = "Shell";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Shell/Shell/Shell_ID";
  incAutoNum(autoNumDest);

  populateFileList("Shell/Shell/Sketch_Plan_Shell", new ArrayList());
  populateCameraPictureGallery("Shell/Shell/Photo_Shell", new ArrayList());


  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();
      excludeAttributes.add("Sketch Plan Shell");
      excludeAttributes.add("Photo Shell");

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteShell(){
  String tabgroup = "Shell";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteShell()", "doNotDelete()");
  }
}

reallyDeleteShell(){
  String tabgroup = "Shell";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newHearthGroundOven(){
  String tabgroup = "Hearth_Ground_Oven";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Hearth_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Hearth_Ground_Oven/Hearth_Ground_Oven/Hearth_ID";
  incAutoNum(autoNumDest);
}

duplicateHearthGroundOven(){
  String tabgroup = "Hearth_Ground_Oven";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Hearth_Ground_Oven/Hearth_Ground_Oven/Hearth_ID";
  incAutoNum(autoNumDest);

  populateFileList("Hearth_Ground_Oven/Hearth_Ground_Oven/Sketch_Plan_Hearth", new ArrayList());
  populateCameraPictureGallery("Hearth_Ground_Oven/Hearth_Ground_Oven/Photo_Hearth", new ArrayList());


  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();
      excludeAttributes.add("Sketch Plan Hearth");
      excludeAttributes.add("Photo Hearth");

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteHearthGroundOven(){
  String tabgroup = "Hearth_Ground_Oven";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteHearthGroundOven()", "doNotDelete()");
  }
}

reallyDeleteHearthGroundOven(){
  String tabgroup = "Hearth_Ground_Oven";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newPotentialArchaeologicalDeposit(){
  String tabgroup = "Potential_Archaeological_Deposit";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Arch_Dep_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Arch_Dep_ID";
  incAutoNum(autoNumDest);
}

duplicatePotentialArchaeologicalDeposit(){
  String tabgroup = "Potential_Archaeological_Deposit";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Arch_Dep_ID";
  incAutoNum(autoNumDest);



  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deletePotentialArchaeologicalDeposit(){
  String tabgroup = "Potential_Archaeological_Deposit";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeletePotentialArchaeologicalDeposit()", "doNotDelete()");
  }
}

reallyDeletePotentialArchaeologicalDeposit(){
  String tabgroup = "Potential_Archaeological_Deposit";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newCulturallyModifiedTree(){
  String tabgroup = "Culturally_Modified_Tree";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_CM_Tree_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Culturally_Modified_Tree/Culturally_Modified_Tree/CM_Tree_ID";
  incAutoNum(autoNumDest);
}

duplicateCulturallyModifiedTree(){
  String tabgroup = "Culturally_Modified_Tree";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Culturally_Modified_Tree/Culturally_Modified_Tree/CM_Tree_ID";
  incAutoNum(autoNumDest);



  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteCulturallyModifiedTree(){
  String tabgroup = "Culturally_Modified_Tree";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteCulturallyModifiedTree()", "doNotDelete()");
  }
}

reallyDeleteCulturallyModifiedTree(){
  String tabgroup = "Culturally_Modified_Tree";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}

newCulturalManagementComment(){
  String tabgroup = "Cultural_Management_Comment";

  String autoNumSource = "";
  autoNumSource = getFieldValue("Control/Control/Next_Comment_ID");
  if (isNull(autoNumSource)) {
    showWarning("{Alert}","{A_next_ID_has_not_been_entered_please_provide_one}");
    return;
  }

  setUuid(tabgroup, null);
  newTabGroup(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Cultural_Management_Comment/C_M_Comments/Comment_ID";
  incAutoNum(autoNumDest);
}

duplicateCulturalManagementComment(){
  String tabgroup = "Cultural_Management_Comment";
  String uuidOld = getUuid(tabgroup);

  disableAutoSave(tabgroup);

  String autoNumDest = "";
  autoNumDest = "Cultural_Management_Comment/C_M_Comments/Comment_ID";
  incAutoNum(autoNumDest);



  saveCallback = new SaveCallback() {
    onSave(uuid, newRecord) {
      setUuid(tabgroup, uuid);
      populateAuthorAndTimestamp(tabgroup);

      Boolean enable_autosave = true;

      fetchAll(getDuplicateRelnQuery(uuidOld), new FetchCallback(){
        onFetch(result) {
          Log.e("Module", result.toString());
          makeDuplicateRelationships(result, getUuid(tabgroup));
          showToast("{Duplicated_record}");
          dialog.dismiss();
        }
      });

      saveTabGroup(tabgroup, getUuid(tabgroup), null, null, new SaveCallback(){
        onSave(autosaveUuid, autosaveNewRecord) {
          setUuid(tabgroup, autosaveUuid);
        }
      }, enable_autosave);
    }
  };

  String extraDupeAttributes = "";
  fetchAll(getDuplicateAttributeQuery(getUuid(tabgroup), extraDupeAttributes), new FetchCallback(){
    onFetch(result) {
      excludeAttributes = new ArrayList();

      duplicateTabGroup(tabgroup, null, getExtraAttributes(result), excludeAttributes, saveCallback);
    }
  });
}

deleteCulturalManagementComment(){
  String tabgroup = "Cultural_Management_Comment";

  if (isNull(getUuid(tabgroup))) {
    cancelTabGroup(tabgroup, true);
  } else {
    showAlert("{Confirm_Deletion}", "{Press_OK_to_Delete_this_Record}", "reallyDeleteCulturalManagementComment()", "doNotDelete()");
  }
}

reallyDeleteCulturalManagementComment(){
  String tabgroup = "Cultural_Management_Comment";
  deleteArchEnt(getUuid(tabgroup));
  cancelTabGroup(tabgroup, false);
}


doNotDelete(){
  showToast("{Delete_Cancelled}");
}
addOnEvent("User", "show", "removeNavigationButtons()");
addOnEvent("Control", "show", "removeNavigationButtons()");
addOnEvent("Transect", "show", "addNavigationButtons(\"Transect\")");
addOnEvent("Site", "show", "addNavigationButtons(\"Site\")");
addOnEvent("Feature", "show", "addNavigationButtons(\"Feature\")");
addOnEvent("Artefact", "show", "addNavigationButtons(\"Artefact\")");
addOnEvent("Intangible_Value", "show", "addNavigationButtons(\"Intangible_Value\")");
addOnEvent("Art", "show", "addNavigationButtons(\"Art\")");
addOnEvent("Other_Feature", "show", "addNavigationButtons(\"Other_Feature\")");
addOnEvent("Grinding_Grove", "show", "addNavigationButtons(\"Grinding_Grove\")");
addOnEvent("Shell", "show", "addNavigationButtons(\"Shell\")");
addOnEvent("Hearth_Ground_Oven", "show", "addNavigationButtons(\"Hearth_Ground_Oven\")");
addOnEvent("Potential_Archaeological_Deposit", "show", "addNavigationButtons(\"Potential_Archaeological_Deposit\")");
addOnEvent("Culturally_Modified_Tree", "show", "addNavigationButtons(\"Culturally_Modified_Tree\")");
addOnEvent("Cultural_Management_Comment", "show", "addNavigationButtons(\"Cultural_Management_Comment\")");

/******************************************************************************/
/*                                   SEARCH                                   */
/******************************************************************************/
addOnEvent("Control/Search"               , "show"  , "search();");
addOnEvent("Control/Search/Entity_List"   , "click" , "loadEntity();");
addOnEvent("Control/Search/Search_Button" , "click" , "search()");
addOnEvent("Control/Search/Search_Term"   , "click" , "clearSearch()");
addOnEvent("Control/Search/Entity_Types"  , "click" , "search()");

entityTypes = new ArrayList();
entityTypes.add(new NameValuePair("{All}", ""));
entityTypes.add(new NameValuePair("{Transect}", "Transect"));
entityTypes.add(new NameValuePair("{Site}", "Site"));
entityTypes.add(new NameValuePair("{Feature}", "Feature"));
entityTypes.add(new NameValuePair("{Artefact}", "Artefact"));
entityTypes.add(new NameValuePair("{Intangible_Value}", "Intangible Value"));
entityTypes.add(new NameValuePair("{Art}", "Art"));
entityTypes.add(new NameValuePair("{Other_Feature}", "Other Feature"));
entityTypes.add(new NameValuePair("{Grinding_Grove}", "Grinding Grove"));
entityTypes.add(new NameValuePair("{Shell}", "Shell"));
entityTypes.add(new NameValuePair("{Hearth_Ground_Oven}", "Hearth Ground Oven"));
entityTypes.add(new NameValuePair("{Potential_Archaeological_Deposit}", "Potential Archaeological Deposit"));
entityTypes.add(new NameValuePair("{Culturally_Modified_Tree}", "Culturally Modified Tree"));
entityTypes.add(new NameValuePair("{Cultural_Management_Comment}", "Cultural Management Comment"));
populateDropDown("Control/Search/Entity_Types", entityTypes);

clearSearch(){
  setFieldValue("Control/Search/Search_Term","");
}

search(){
  String tabgroup = "Control";
  String refEntityList  = tabgroup + "/Search/Entity_List";
  String refSearchTerm  = tabgroup + "/Search/Search_Term";
  String refEntityTypes = tabgroup + "/Search/Entity_Types";

  String type = getFieldValue(refEntityTypes);
  String term = getFieldValue(refSearchTerm);
  String searchQuery = "SELECT uuid, response "+
                       "  FROM latestNonDeletedArchEntFormattedIdentifiers  "+
                       " WHERE uuid in (SELECT uuid "+
                       "                  FROM latestNonDeletedArchEntIdentifiers "+
                       "                 WHERE measure LIKE '"+term+"'||'%'  "+
                       "                   AND ( aenttypename LIKE '"+type+"' OR '' = '"+type+"' ) "+
                       "                )  "+
                       " ORDER BY response "+
                       " LIMIT ? "+
                       "OFFSET ? ";

  populateCursorList(refEntityList, searchQuery, 25);
  refreshTabgroupCSS(tabgroup);

  Log.d("Module", "Search query: " + searchQuery);
}

loadTransectFrom(String uuid) {
  String tabgroup = "Transect";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadSiteFrom(String uuid) {
  String tabgroup = "Site";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadFeatureFrom(String uuid) {
  String tabgroup = "Feature";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadArtefactFrom(String uuid) {
  String tabgroup = "Artefact";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadIntangibleValueFrom(String uuid) {
  String tabgroup = "Intangible_Value";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadArtFrom(String uuid) {
  String tabgroup = "Art";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadOtherFeatureFrom(String uuid) {
  String tabgroup = "Other_Feature";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadGrindingGroveFrom(String uuid) {
  String tabgroup = "Grinding_Grove";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadShellFrom(String uuid) {
  String tabgroup = "Shell";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadHearthGroundOvenFrom(String uuid) {
  String tabgroup = "Hearth_Ground_Oven";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadPotentialArchaeologicalDepositFrom(String uuid) {
  String tabgroup = "Potential_Archaeological_Deposit";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadCulturallyModifiedTreeFrom(String uuid) {
  String tabgroup = "Culturally_Modified_Tree";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}

loadCulturalManagementCommentFrom(String uuid) {
  String tabgroup = "Cultural_Management_Comment";
  setUuid(tabgroup, uuid);
  if (isNull(uuid)) return;

  showTabGroup(tabgroup, uuid);
}


/******************************************************************************/
/*                          TAKE FROM GPS BUTTON(S)                           */
/******************************************************************************/
addOnEvent("Transect/Transect/Take_From_GPS", "click", "takePoint(\"Transect\")");

/* Takes the current point using gps. */
takePoint(String tabgroup) {
  String archEntType = tabgroup.replaceAll("_", " ");
  String currentUuid = getUuid(tabgroup);
  if (isNull(currentUuid)){
    showToast("Please enter data first and let a save occur.");
  }

  Object position = getGPSPosition();
  if (position == null) {
    showToast("{GPS_Not_Initialised}");
    return;
  }

  Object projPosition = getGPSPositionProjected();
  Double latitude     = position.getLatitude();
  Double longitude    = position.getLongitude();
  Double northing     = projPosition.getLatitude();
  Double easting      = projPosition.getLongitude();

  samplePoint = new Point(new MapPos(easting, northing), null, (PointStyle) null, null);
  ArrayList geolist = new ArrayList();
  geolist.add(samplePoint);

  attributes = createAttributeList();
  attributes.add(createEntityAttribute("Latitude", "Accuracy: "+getGPSEstimatedAccuracy(), null, null, null));

  saveArchEnt(currentUuid, archEntType, geolist, attributes, new SaveCallback() {
    onSave(uuid, newRecord) {
      print("[takePoint()] Added geometry: " + geolist);
      fillInGPS(tabgroup);
    }
  });
}

/* Sets the value of GPS views for the given tab path. */
fillInGPS(String tabgroup) {
  Map tabgroupToTabRef = new HashMap();
  tabgroupToTabRef.put("Transect", "Transect/Transect/");

  String currentUuid = getUuid(tabgroup);
  if (isNull(currentUuid)) {
    return;
  }

  String query = "SELECT x(transform(geospatialcolumn,                4326)) as longtiude, " +
                 "       y(transform(geospatialcolumn,                4326)) as latitude, " +
                 "       x(transform(geospatialcolumn, "+getModuleSrid()+")) as easting, " +
                 "       y(transform(geospatialcolumn, "+getModuleSrid()+")) as northing " +
                 "  FROM latestnondeletedarchent, vocabulary " +
                 " WHERE uuid = '" + currentUuid + "';";

  fetchOne(query, new FetchCallback() {
    onFetch(result) {
      print("[fillInGPS()] Fetched DB transformed geometry: " + result);
      setFieldValue(tabgroupToTabRef.get(tabgroup) + "Longitude" , result.get(0));
      setFieldValue(tabgroupToTabRef.get(tabgroup) + "Latitude"  , result.get(1));
      setFieldValue(tabgroupToTabRef.get(tabgroup) + "Easting"   , result.get(2));
      setFieldValue(tabgroupToTabRef.get(tabgroup) + "Northing"  , result.get(3));
    }
  });
}

/******************************************************************************/
/*                       AUTONUMBERING HELPER FUNCTIONS                       */
/******************************************************************************/
/*
 * If value of field specified by `ref` is null, sets the field to `defaultVal`,
 * otherwise increments its value.
 *
 * Returns the value the field was updated to.
 */
incField(String ref, Integer defaultVal) {
  String val = getFieldValue(ref);

  if (isNull(val)) {
    setFieldValue(ref, defaultVal);
    return defaultVal;
  }

  Integer inc = Integer.parseInt(val) + 1;
  setFieldValue(ref, inc);
  insertIntoLocalSettings(ref, inc.toString());

  return inc;
}

/* Increments the field at `ref` or returns null if it does not contain a
 * number.
 */
incField(String ref) {
  return incField(ref, 1);
}

addOnEvent("Control/Control", "show", "onShowAutonum()");

/* This function should only be called once since it creates event handlers,
 * otherwise multiple copies of the same handler will trigger with the event.
 */
onShowAutonum() {
  List l = new ArrayList();
  l.add("Control/Control/Next_Transect_ID");
  l.add("Control/Control/Next_Site_ID_Number");
  l.add("Control/Control/Next_Feature_ID");
  l.add("Control/Control/Next_Intangible_Value_ID");
  l.add("Control/Control/Next_Art_ID");
  l.add("Control/Control/Next_Other_Feature_ID");
  l.add("Control/Control/Next_Grinding_Grove_ID");
  l.add("Control/Control/Next_Shell_ID");
  l.add("Control/Control/Next_Hearth_ID");
  l.add("Control/Control/Next_Arch_Dep_ID");
  l.add("Control/Control/Next_CM_Tree_ID");
  l.add("Control/Control/Next_Comment_ID");


  for (ref : l) {
    loadStartingId(ref);
  }
  for (ref : l) {
    onFocus(ref, null,  "insertIntoLocalSettings(\"" + ref + "\", getFieldValue(\"" + ref + "\"));");
  }
}

loadStartingId(String ref) {
  String idQ = "SELECT value FROM localSettings WHERE key = '" + ref + "';";
  fetchOne(idQ, new FetchCallback() {
    onFetch(result) {
      if (!isNull(result)) {
        setFieldValue(ref, result.get(0));
      } else {
        setFieldValue(ref, "1");
      }
    }
  });
}

incAutoNum(String destPath) {
  Map destToSource = new HashMap();
  destToSource.put("Transect/Transect/Transect_ID", "Control/Control/Next_Transect_ID");
  destToSource.put("Site/Site/Site_ID_Number", "Control/Control/Next_Site_ID_Number");
  destToSource.put("Feature/Feature/Feature_ID", "Control/Control/Next_Feature_ID");
  destToSource.put("Intangible_Value/Intangible_Value/Intangible_Value_ID", "Control/Control/Next_Intangible_Value_ID");
  destToSource.put("Art/Art/Art_ID", "Control/Control/Next_Art_ID");
  destToSource.put("Other_Feature/Other_Feature/Other_Feature_ID", "Control/Control/Next_Other_Feature_ID");
  destToSource.put("Grinding_Grove/Grinding_Grove/Grinding_Grove_ID", "Control/Control/Next_Grinding_Grove_ID");
  destToSource.put("Shell/Shell/Shell_ID", "Control/Control/Next_Shell_ID");
  destToSource.put("Hearth_Ground_Oven/Hearth_Ground_Oven/Hearth_ID", "Control/Control/Next_Hearth_ID");
  destToSource.put("Potential_Archaeological_Deposit/Potential_Archaeological_Deposit/Arch_Dep_ID", "Control/Control/Next_Arch_Dep_ID");
  destToSource.put("Culturally_Modified_Tree/Culturally_Modified_Tree/CM_Tree_ID", "Control/Control/Next_CM_Tree_ID");
  destToSource.put("Cultural_Management_Comment/C_M_Comments/Comment_ID", "Control/Control/Next_Comment_ID");

  String sourcePath = destToSource.get(destPath);
  String destVal    = getFieldValue(sourcePath);
  setFieldValue(destPath, destVal);
  incField(sourcePath);
}

/******************************************************************************/
/*                POPULATION OF ENTITY AND CHILD ENTITY LISTS                 */
/******************************************************************************/
/*
 * `viewType`   the type of GUI element to be populated. It can either equal
 *              "DropDown" or "List".
 * `path`       the reference of the GUI element to be populated.
 * `parentUuid` the parent in the relationship denoted by `relType`.
 * `entType`    the type of the entities the menu will be populated with.
 * `relType`    the name of the relationship the children are to be in with the
 *              entity denoted by `parentUuid`.
 */
populateMenuWithEntities (
  String viewType,
  String path,
  String parentUuid,
  String entType,
  String relType
) {
  String getChildEntitiesQ = "" +
    "SELECT childuuid, response "+
    "  FROM parentchild JOIN latestNonDeletedArchEntFormattedIdentifiers ON (childuuid = uuid) " +
    "  JOIN createdmodifiedatby USING (uuid) " +
    " WHERE relationshipid IN (SELECT relationshipid  " +
    "                            FROM latestnondeletedrelationship JOIN relntype USING (relntypeid) " +
    "                           WHERE relntypename = '"+relType+"') " +
    "   AND parentuuid = " + parentUuid + " " +
    "   AND (childaenttypename = '"+entType+"' OR '"+entType+"' = '') " +
    " ORDER BY createdat DESC ";

  String getEntitiesQ = "" +
    "SELECT uuid, response "+
    "  FROM latestNonDeletedArchEntFormattedIdentifiers  "+
    " WHERE uuid in (SELECT uuid "+
    "                  FROM latestNonDeletedArchEntIdentifiers "+
    "                 WHERE aenttypename = '"+entType+"' OR '"+entType+"' = '' " +
    "               )  "+
    " ORDER BY response ";

  String q = null;
  if (relType.equals("")) {
    q = getEntitiesQ;
  } else {
    q = getChildEntitiesQ;
  }

  FetchCallback cbPopulateDropDown = new FetchCallback() {
    onFetch(result) {
      populateDropDown(path, result, true);
    }
  };

  switch (viewType) {
    case "DropDown":
      fetchAll(q, cbPopulateDropDown);
      break;
    case "List":
      q += " LIMIT ? OFFSET ? ";
      populateCursorList(path, q, 25);
      break;
    default:
      Log.e("populateMenuWithEntities ", "Unexpected type '" + viewType + "'");
  }
}

menus = new ArrayList();
menus.add(new String[] {
  "List",
  "Site/Features/List_of_Features",
  "getUuid(\"Site\")",
  "Feature",
  "Site - Feature",
});
menus.add(new String[] {
  "List",
  "Feature/Artefact/List_of_Artefact_Records",
  "getUuid(\"Feature\")",
  "Artefact",
  "Feature - Artefact",
});
menus.add(new String[] {
  "List",
  "Feature/Intangible_Value/List_of_Intangible_Value_Records",
  "getUuid(\"Feature\")",
  "Intangible Value",
  "Feature - Intangible Value",
});
menus.add(new String[] {
  "List",
  "Feature/Art/List_of_Art_Records",
  "getUuid(\"Feature\")",
  "Art",
  "Feature - Art",
});
menus.add(new String[] {
  "List",
  "Feature/Other_Feature/List_of_Other_Feature_Records",
  "getUuid(\"Feature\")",
  "Other Feature",
  "Feature - Other Feature",
});
menus.add(new String[] {
  "List",
  "Feature/Grinding_Grove/List_of_Grinding_Grove_Records",
  "getUuid(\"Feature\")",
  "Grinding Grove",
  "Feature - Grinding Grove",
});
menus.add(new String[] {
  "List",
  "Feature/Shell/List_of_Shell_Records",
  "getUuid(\"Feature\")",
  "Shell",
  "Feature - Shell",
});
menus.add(new String[] {
  "List",
  "Feature/Hearth_Ground_Oven/List_of_Hearth_Ground_Oven_Records",
  "getUuid(\"Feature\")",
  "Hearth Ground Oven",
  "Feature - Hearth Ground Oven",
});
menus.add(new String[] {
  "List",
  "Feature/Potential_Archaeological_Deposit/List_of_Potential_Archaeological_Deposit_Records",
  "getUuid(\"Feature\")",
  "Potential Archaeological Deposit",
  "Feature - Potential Archaeological Deposit",
});
menus.add(new String[] {
  "List",
  "Feature/Culturally_Modified_Tree/List_of_Culturally_Modified_Tree_Records",
  "getUuid(\"Feature\")",
  "Culturally Modified Tree",
  "Feature - Culturally Modified Tree",
});
for (m : menus) {
  String viewType       = m[0];
  String path           = m[1];
  String parentUuidCall = m[2];
  String entType        = m[3];
  String relType        = m[4];

  String functionCall = "";
  functionCall += "populateMenuWithEntities(";
  functionCall += "\"" + viewType       + "\"";
  functionCall += ", ";
  functionCall += "\"" + path           + "\"";
  functionCall += ", ";
  functionCall +=        parentUuidCall       ;
  functionCall += ", ";
  functionCall += "\"" + entType        + "\"";
  functionCall += ", ";
  functionCall += "\"" + relType        + "\"";
  functionCall += ")";

  addOnEvent(path, "show", functionCall);
}
addOnEvent("Site/Features/List_of_Features", "click", "loadEntity()");
addOnEvent("Feature/Artefact/List_of_Artefact_Records", "click", "loadEntity()");
addOnEvent("Feature/Intangible_Value/List_of_Intangible_Value_Records", "click", "loadEntity()");
addOnEvent("Feature/Art/List_of_Art_Records", "click", "loadEntity()");
addOnEvent("Feature/Other_Feature/List_of_Other_Feature_Records", "click", "loadEntity()");
addOnEvent("Feature/Grinding_Grove/List_of_Grinding_Grove_Records", "click", "loadEntity()");
addOnEvent("Feature/Shell/List_of_Shell_Records", "click", "loadEntity()");
addOnEvent("Feature/Hearth_Ground_Oven/List_of_Hearth_Ground_Oven_Records", "click", "loadEntity()");
addOnEvent("Feature/Potential_Archaeological_Deposit/List_of_Potential_Archaeological_Deposit_Records", "click", "loadEntity()");
addOnEvent("Feature/Culturally_Modified_Tree/List_of_Culturally_Modified_Tree_Records", "click", "loadEntity()");

/******************************************************************************/
/*                             HANDWRITTEN LOGIC                              */
/******************************************************************************/


/******************************************************************************/
/*                                    INIT                                    */
/*                                                                            */
/* Stuff which needs to be done last.                                         */
/******************************************************************************/
bindOnEvents();
